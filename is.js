AOS.init({
    duration: 900
   });
// var height = $(window).height();//gets height from device
// var width = $(window).width(); //gets width from device

// $(".top-container").width(width+"px");
// $(".top-container").height(height+"px");